[package]
name = "hormos-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Cœur d'Hormos : domaine, validation, erreurs et abstraction ContainerRuntime. Aucune dépendance à un moteur de conteneurs."
readme = "../../README.md"

[dependencies]
async-trait.workspace = true
serde.workspace = true
thiserror.workspace = true

# `mock` expose le faux moteur déterministe aux tests des autres crates du
# workspace. Aucune crate ne l'active en dépendance normale : il n'entre donc
# jamais dans le binaire `hormos`.
[features]
mock = []

[dev-dependencies]
serde_json.workspace = true
tokio = { workspace = true, features = ["macros", "rt"] }

[lints]
workspace = true
